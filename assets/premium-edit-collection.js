/* Premium Edit Collection Section JS */
document.addEventListener('DOMContentLoaded', () => {
  const premiumSections = document.querySelectorAll('.premium-edit-section');
  if (premiumSections.length === 0) return;

  console.log('Premium Edit Collection Section loaded.');
});
