/* Custom Footer JS */
document.addEventListener('DOMContentLoaded', () => {
  const customFooter = document.querySelector('.custom-footer');
  if (!customFooter) return;

  // Add any custom footer interactions here
  console.log('Custom footer loaded.');
});
